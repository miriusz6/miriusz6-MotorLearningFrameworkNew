using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class OptionToggle : Option
{
    public bool hasLabel = false;
    public string sliderLabel = "Optional Slider Label";

    public string[] sliderValuesLabels = new string[] { "Low", "Medium", "High" };

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
